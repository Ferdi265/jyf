# Ich hatte Zeit und hab einen Interpreter
# für eine eigene Sprache in JavaScript implementiert

print("hello")

dec('a 5)

print("a ist" a)

dec('fib function(('n)
	if(<(n 2)
		1
		+(fib(-(n 1)) fib(-(n 2)))
	)
))

for((dec('i 0) <(i 10) inc('i))
	print(fib(i))
)
